# OTask Mobile

Flutter Android/iOS application shell. The generated platform projects and
Flutter analysis/test checks are created in `P00-007`; Flutter is currently
unavailable in the bootstrap environment.
