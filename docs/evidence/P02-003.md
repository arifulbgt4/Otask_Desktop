# Task Evidence: P02-003

- Task: create execution run/event/log/artifact/approval/grant tables.
- Added migration `202607130003_runs_evidence_grants.sql` and pgTAP test
  `supabase/tests/p02_003_runs.sql`.
- Design controls:
  - runs bind immutable workflow versions, target devices, plan hashes, and
    idempotency keys;
  - step events/log chunks are append-only sequenced records;
  - artifacts are content-addressed, bounded, and redaction-aware;
  - approvals bind user/run/scope/plan hash/expiry;
  - grants bind user/device/run/plan scope, nonce, signature, and expiry.
- RLS: authenticated users can read only their own target-bound runs and
  evidence; service-owned event/log/artifact/grant writes have no client write
  policy.
- Status: `IN_PROGRESS` until required remote CI is green.
