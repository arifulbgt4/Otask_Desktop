# Task Evidence: P01-008

- Task: add compatibility policy and fixture tests for versioned contracts.
- Changed files:
  - `packages/contracts/schemas/compatibility.schema.json`
  - `packages/contracts/fixtures/compatibility.*.json`
  - `packages/contracts/src/compatibility.ts`
  - `packages/contracts/scripts/check-compatibility.mjs`
  - updated TypeScript/Rust/Dart schema manifests
  - `docs/COMPATIBILITY.md`
- Validation:
  - `pnpm --filter @otask/contracts check` passed: 14 schemas and 27
    fixtures; 14 schemas mapped in all three languages; current/unsupported
    version fixtures passed.
  - `pnpm -r check` passed.
  - `pnpm exec prettier --check packages/contracts docs/COMPATIBILITY.md` and
    `git diff --check` passed.
- Acceptance mapping: supported `1.0` is accepted, unknown `2.0` is rejected
  before deserialization, and schema additions cannot bypass mapping guards.
- Security review: compatibility checks reject tampered/future executable
  contracts instead of silently coercing them.
- Status: `IN_PROGRESS` until required remote CI is green.
