# Task Evidence: P01-002

- Task: define the immutable executable `TaskPlan` and all v1 typed steps.
- Changed files:
  - `packages/contracts/schemas/task-plan.schema.json`
  - `packages/contracts/fixtures/task-plan.*.json`
  - `packages/contracts/scripts/check.mjs`
- Step coverage: `open_workspace`, `open_application`,
  `run_command_template`, `run_package_script`, `wait_port`, `wait_process`,
  `wait_file`, `open_url`, `copy_clipboard`, `notify`, `delay`, and
  `collect_artifact`.
- Validation:
  - `pnpm --filter @otask/contracts check` passed: 4 schemas and 7 fixtures.
  - The valid fixture exercises every v1 step type.
  - Negative fixtures reject unknown properties and dependency cycles.
  - `pnpm exec prettier --check packages/contracts` passed.
  - `git diff --check` passed.
- Acceptance mapping: every step uses a discriminated `type`, strict typed
  parameters, bounded time/size controls, explicit success criteria, optional
  evidence capture, and no arbitrary executable command field.
- Security review: command execution is represented only by a saved template
  reference or package-manager/script tuple; raw paths and model execution
  flags are rejected.
- Remote validation for `4385afb02d14caae0c8baa3ffc7f8e7bfc44721c`: Contracts,
  Web, Rust, Mobile, Supabase, and Security all completed successfully.
- Status: `DONE`.
