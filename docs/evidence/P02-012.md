# Task Evidence: P02-012

- Task: add local reset/seed/RLS/Edge Function CI coverage.
- Added an idempotent `supabase/seed.sql` with private Storage buckets and a
  published learning fixture, plus pgTAP checks for seed visibility, RLS
  coverage, service-only function privileges, and reset-created tables.
- Added `scripts/check-supabase-ci.mjs` and `pnpm supabase:check` to verify all
  configured Edge Functions, JWT boundaries, CORS/method guards, seed hooks,
  and the Supabase reset/test/stop workflow.
- Remote validation: pending until all six required checks pass for the final
  implementation head.
- Status: `IN_PROGRESS`.
