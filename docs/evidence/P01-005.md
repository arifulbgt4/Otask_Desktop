# Task Evidence: P01-005

- Task: maintain tested internal TypeScript, Rust, and Dart mappings for the
  versioned contract schemas.
- Changed files:
  - `packages/contracts/src/generated.ts`
  - `crates/otask-domain/src/lib.rs`
  - `apps/mobile/lib/contracts.dart`
  - `packages/contracts/scripts/check-mappings.mjs`
  - `packages/contracts/package.json`
- Validation:
  - `pnpm --filter @otask/contracts check` passed: 13 schemas and 25 fixtures,
    plus all 13 schema names mapped in TypeScript, Rust, and Dart.
  - `git diff --check` passed.
  - Rust formatting/clippy/tests and Flutter format/analyze/tests are delegated
    to the required remote platform jobs because the local toolchains are not
    installed.
- Acceptance mapping: each target language carries the same schema manifest,
  version marker, execution state vocabulary, and legal transition helper;
  the mapping guard fails if a schema is added without all three mappings.
- Security review: mappings contain no credentials, payloads, filesystem
  access, or execution capability.
- Remote validation for `15b5dfbbb2bb0021787bca0a95cc00a15158bc52`: Contracts,
  Web, Rust, Mobile, Supabase, and Security all completed successfully.
- Status: `DONE`.
