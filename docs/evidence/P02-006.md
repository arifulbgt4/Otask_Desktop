# Task Evidence: P02-006

- Task: configure Google authentication for local/dev and define safe callback
  handling for web and native clients.
- `supabase/config.toml` enables Google through env-backed client credentials,
  uses the local GoTrue callback URL, exact web/custom-scheme redirect
  allowlists, and keeps nonce validation enabled.
- `supabase/.env.example` documents placeholders without credentials. No real
  client secret, service-role key, or token is committed.
- Added `@otask/auth` callback parser and redirect fixtures, Flutter callback
  parsing tests, `docs/AUTH.md`, and `pnpm auth:check` CI validation. The
  callback contract accepts authorization-code + state only and rejects
  implicit-flow fragments, missing state, errors, and unregistered redirects.
- Live Google sign-in requires developer-supplied OAuth credentials in the
  untracked local env; CI intentionally validates configuration and callback
  boundaries without handling a real account.
- Remote validation: all six required checks passed for final head `1ed4160`
  (Contracts, Web, Rust, Mobile, Supabase, Security). The Supabase job
  completed start, reset, pgTAP, and stop with the Google provider config
  enabled and no committed credentials.
- Status: `DONE` for local/dev provider configuration and callback contract.
  A real-account Google smoke test remains credential-dependent and is covered
  by the later web/native client tasks.
