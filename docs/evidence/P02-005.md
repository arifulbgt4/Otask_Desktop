# Task Evidence: P02-005

- Task: harden and test RLS for every exposed table, including cross-user and
  revoked-device boundaries.
- Added migration `202607130005_rls_hardening.sql` to bind device actions to
  the authenticated owner and require `devices.status <> 'revoked'` for new
  schedules, runs, approvals, and clipboard writes. Revocation remains a
  trusted service operation; clients cannot mutate a revoked device back into
  an active state.
- The pgTAP suites check RLS on all exposed tables, verify the policy
  predicates contain the revoked-device guard, assert owner/device policy
  inventories and service-only write boundaries, and run two-user runtime
  fixtures proving cross-user reads/writes and revoked-device clipboard/run
  dispatches are rejected.
- Status: `IN_PROGRESS` until all required remote CI checks are green.
