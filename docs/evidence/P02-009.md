# Task Evidence: P02-009

- Task: implement signed plan/approval/target-bound run grants.
- Added `issue_run_grant` and `consume_run_grant` service-only SQL functions.
  Issuance is atomic and checks the immutable workflow-version hash, run hash,
  owner-bound trusted target, unrevoked approval, short expiry, and exact scope
  bindings. Consumption rejects signature/nonce mismatches, expiry, revocation,
  wrong targets, and replay through `used_at`.
- Added authenticated `issue-run-grant` and `consume-run-grant` Edge Functions,
  canonical HMAC signing with an env-backed secret, validation fixtures, pgTAP
  privilege/constraint coverage, and runtime issue/consume/replay tests.
- Remote validation: all six required checks passed for final head `d51a8a4`
  (Contracts run `29249826516`, Web `29249826563`, Rust `29249826496`, Mobile
  `29249826564`, Supabase `29249826548`, Security `29249826503`). Supabase reset,
  pgTAP, and all 10 run-grant runtime assertions passed.
- Status: `DONE`.
