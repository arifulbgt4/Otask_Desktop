# Task Evidence: P02-002

- Task: create workflow, immutable workflow-version, and target-bound schedule
  migrations.
- Changed files:
  - `supabase/migrations/202607130002_workflows_schedules.sql`
  - `supabase/tests/p02_002_workflows.sql`
  - `supabase/migrations/README.md`
- Validation design:
  - workflow versions have unique `(workflow_id, version)` and a trigger that
    rejects update/delete; edits require a new version.
  - schedules bind an exact workflow version and target device, with timezone,
    trigger, approval, lateness, retry, precondition, enabled, and revision
    fields.
  - pgTAP checks tables, RLS, exact owner policies, and immutable trigger;
    `supabase test db` will run it in CI.
- Security review: workflow/version/schedule policies require the same
  authenticated owner and schedule target-device owner; no client update/delete
  policy exists for immutable versions.
- Remote validation for `dc6ca42eafff894605f4188f66bede9f179410a4`: Contracts,
  Web, Rust, Mobile, Supabase (including both pgTAP suites), and Security all
  completed successfully.
- Status: `DONE`.
