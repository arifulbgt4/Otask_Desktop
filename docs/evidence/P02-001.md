# Task Evidence: P02-001

- Task: create profiles/devices/device-keys/capabilities migrations and owner
  scoped RLS.
- Changed files:
  - `supabase/migrations/202607130001_profiles_devices.sql`
  - `supabase/tests/p02_001_rls.sql`
  - `.github/workflows/ci-supabase.yml`
- Validation:
  - Migration includes ID/version constraints, UTC timestamps, update
    triggers, indexes, public-key-only storage, capability normalization, and
    RLS on every exposed table.
  - pgTAP test checks table existence, RLS enablement, and exact policy sets;
    CI now runs `supabase test db` after local reset.
  - Local Supabase CLI is unavailable; remote Supabase CI is pending.
- Security review: private keys are not modeled, device-key/capability writes
  have no authenticated-client policy, user policies bind through `auth.uid()`,
  and device inserts are limited to pairing status.
- Status: `IN_PROGRESS` until required remote CI is green.
