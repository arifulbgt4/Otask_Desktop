# Task Evidence: P02-007

- Task: implement `register-device` and pairing-challenge functions with
  same-user proof and expiry controls.
- Added `device_pairing_challenges` migration with hashed, short-lived,
  single-use challenges, five-attempt limit, owner-readable status, and
  service-only writes.
- Added Supabase Edge Functions:
  - `register-device`: authenticates the bearer session, validates the device
    envelope and Ed25519 public key, creates a `pairing` device/key, and returns
    a one-time challenge that is never stored in plaintext.
  - `pairing-challenge`: authenticates the same user, checks ownership,
    expiry/consumption/attempt limits, verifies the Ed25519 signature, consumes
    the challenge, and promotes the device to `trusted`.
- `scripts/check-device-functions.mjs` covers invalid input, key algorithm,
  challenge/signature shape, and deterministic hash fixtures. Supabase pgTAP
  covers table/RLS/index/privilege boundaries.
- Status: `IN_PROGRESS` until all required remote CI checks are green.
