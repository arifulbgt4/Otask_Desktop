# Task Evidence: P02-011

- Task: implement release metadata and signed-download URL function.
- Added a service-only published/non-revoked/future-date gate for
  `release_artifacts` and an anonymous `signed-download-url` Edge Function that
  creates a 30–600 second private Storage signed URL from the constrained
  `storage://releases/...` reference.
- Added TTL/ID validation fixtures, pgTAP privilege/metadata tests, and runtime
  coverage for published, draft, revoked, and missing artifacts.
- Corrected PostgreSQL-incompatible `{1,512}` storage-reference regex bounds
  with explicit `char_length` checks and fixed the revoked-artifact runtime
  fixture to provide its required `revoked_at` timestamp.
- Remote validation for final head `08d91de`:
  - Contracts: `29251766577` — success
  - Web: `29251766441` — success
  - Rust: `29251766460` — success
  - Mobile: `29251766471` — success
  - Supabase: `29251766528` — success; reset, pgTAP privilege tests, and
    published/draft/revoked/missing runtime assertions passed
  - Security: `29251766440` — success
- Status: `DONE`.
