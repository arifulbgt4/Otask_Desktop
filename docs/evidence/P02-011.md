# Task Evidence: P02-011

- Task: implement release metadata and signed-download URL function.
- Added a service-only published/non-revoked/future-date gate for
  `release_artifacts` and an anonymous `signed-download-url` Edge Function that
  creates a 30–600 second private Storage signed URL from the constrained
  `storage://releases/...` reference.
- Added TTL/ID validation fixtures, pgTAP privilege/metadata tests, and runtime
  coverage for published, draft, revoked, and missing artifacts.
- Remote validation: pending until all six required checks pass for the final
  implementation head.
- Status: `IN_PROGRESS`.
