# Task Evidence: P01-001

- Task: define versioned IDs, UTC timestamps, revisions, error envelopes, and
  event envelopes.
- Changed files:
  - `packages/contracts/schemas/common.schema.json`
  - `packages/contracts/schemas/error-envelope.schema.json`
  - `packages/contracts/schemas/event-envelope.schema.json`
  - `packages/contracts/src/index.ts`
  - `packages/contracts/fixtures/*.json`
  - `packages/contracts/scripts/check.mjs`
- Validation:
  - `pnpm install --frozen-lockfile` passed.
  - `pnpm --filter @otask/contracts check` passed: 3 schemas and 4 fixtures.
  - `pnpm --filter @otask/contracts typecheck` passed.
  - `pnpm exec prettier --check packages/contracts` passed.
  - `git diff --check` passed.
- Acceptance mapping: positive and negative fixtures cover malformed IDs,
  non-UTC timestamps, invalid error codes, illegal event types, negative
  revisions, unknown producers, and unexpected properties.
- Security review: identifiers are opaque and namespaced; event payloads are
  data-only objects; no model or executor capability is introduced.
- CI correction: security checkout now fetches full history so gitleaks can
  resolve the PR first-parent range; the prior failure was a shallow-checkout
  range error, not a detected secret.
- Remote validation for `58770ba5d33fbbefaa2ce31b2a236a35740c75f4`: Contracts,
  Web, Rust, Mobile, Supabase, and Security all completed successfully.
- Status: `DONE`.
