# Task Evidence: P01-003

- Task: define execution state/event contracts and the authoritative legal
  transition table.
- Changed files:
  - `packages/contracts/schemas/execution-state.schema.json`
  - `packages/contracts/schemas/execution-event.schema.json`
  - `packages/contracts/src/index.ts`
  - `packages/contracts/fixtures/execution-*.json`
  - `packages/contracts/scripts/check.mjs`
- State lifecycle encoded: `draft -> validated -> awaiting_approval|queued ->
running -> success|failed|cancelled|timeout -> evidence_finalized -> synced`,
  with explicit rejected/expired/retry branches and terminal states.
- Validation:
  - `pnpm --filter @otask/contracts check` passed: 6 schemas and 11 fixtures.
  - Valid and invalid transition fixtures exercise cycles, terminal-state
    writes, and legal retry paths.
  - `pnpm exec prettier --check packages/contracts` passed.
  - `git diff --check` passed.
- Security review: events carry opaque run IDs, monotonic sequence numbers,
  actor identity, and data-only payloads; model is not an actor and cannot
  authorize a transition.
- Status: `IN_PROGRESS` until required remote CI is green.
