# Task Evidence: P01-004

- Task: define versioned schedule, approval, device, grant, evidence,
  artifact, and clipboard contracts.
- Added schemas:
  - `schedule.schema.json`
  - `approval.schema.json`
  - `device.schema.json`
  - `run-grant.schema.json`
  - `evidence.schema.json`
  - `artifact.schema.json`
  - `clipboard.schema.json`
- Validation:
  - `pnpm --filter @otask/contracts check` passed: 13 schemas and 25
    fixtures.
  - Every new contract has a positive and negative example.
  - `pnpm exec prettier --check packages/contracts` passed.
  - `git diff --check` passed.
- Acceptance mapping: schedules include timezone/trigger/lateness/retry and
  preconditions; approvals bind decision/scope/plan hash/expiry; grants are
  short-lived and target-bound; evidence/artifacts are content-addressed and
  redaction-aware; clipboard contracts carry only a payload reference and
  expiry, never raw secret text.
- Security review: hashes, signatures, expiry, ownership IDs, bounded sizes,
  and strict additional-property rules are required; binary clipboard data and
  unscoped storage URLs are rejected.
- Status: `IN_PROGRESS` until required remote CI is green.
