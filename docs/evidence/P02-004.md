# Task Evidence: P02-004

- Task: create clipboard, sync, notification, model, release, learning, and
  audit metadata tables.
- Added migration `202607130004_clipboard_model_release_learning_audit.sql` and
  pgTAP test `supabase/tests/p02_004_clipboard_model_release.sql`.
- Design controls:
  - clipboard records contain only encrypted payload references, hashes,
    sensitivity, TTL, pinning, and deletion tombstones; binary clipboard data
    and silent mobile monitoring remain out of scope;
  - sync and audit records are append-only revision/event ledgers;
  - model and release metadata require checksums/signatures and explicit
    approval/published channel states;
  - model installation state is device-bound and cannot be active before
    verification;
  - learning access is explicitly public or authenticated and published-only.
- RLS is enabled on every new table. Client writes are limited to owner clipboard
  and notification state; service-owned sync, model, release, learning, and
  audit writes have no client write policy.
- Retention: clipboard/sync/notification/audit rows carry expiry or retention
  deadlines; cleanup jobs will be added in the later sync/operations tasks.
- Remote validation: all six required checks passed for commit `1de090f`
  (Contracts, Web, Rust, Mobile, Supabase, Security). Supabase start/reset,
  pgTAP, and stop completed successfully.
- Status: `DONE`.
